package com.github.android_login.manager.notification;

public class NotificationState implements Notification {
    public State wifi = new State(false);
    public State bluetooth = new State(false);
}
